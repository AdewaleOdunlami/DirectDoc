@model DirectDoc2.Models.PostalAddress
@using DirectDoc2.DAL;

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>PostalAddress</h4>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @*@Html.DisplayNameFor(model => model.Person.FullName)*@
            Main member
        </dt>

       @* <dd>*@
            @*@Html.DisplayFor(model => model.Person.FullName)*@
            @{
                ClinicContext db = new ClinicContext();
                var fullName = from f in db.Clients
                                where f.ID == Model.PersonID
                                select f;
                
                if(fullName.Any())
                {
                    foreach(var p in fullName)
                    {
                        <dd>@p.FullName</dd>
                    } 
                }
                else
                {
                    
                }
                        
            }
        @*</dd>*@

        <dt>
            @Html.DisplayNameFor(model => model.BoxNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BoxNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.City)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.City)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Country)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Country)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PostalAddressID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
