@model IEnumerable<DirectDoc2.Models.PostalAddress>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Person.Title)*@
            Address Owner
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoxNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoxNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostalAddressID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostalAddressID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostalAddressID })
        </td>
    </tr>
}

</table>
